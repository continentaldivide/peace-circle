# Generated by `npm run types` from the database schema. Formatting it here
# would be undone on every regeneration, so leave it as the generator emits it.
lib/supabase/database.types.ts

# Build output and dependencies
.next/
node_modules/

# Contains a tool-managed block (nextjs-agent-rules) that must stay
# byte-identical; formatting it rewrites inside those markers.
AGENTS.md
